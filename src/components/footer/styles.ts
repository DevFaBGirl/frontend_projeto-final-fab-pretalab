import styled from 'styled-components';

export const StyledFooter = styled.footer`
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background-color: rgb(40, 36, 71);
  color: rgba(255, 255, 255, 0.7); /* Aplica a opacidade ao texto */
  text-align: center;
  padding: 1rem;
  font-size: 0.870rem; /* Ajuste o tamanho da fonte */
  
`;